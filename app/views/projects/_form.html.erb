<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area(:notes, :size => "40x5") %>
  </div>
    <div class="field">
    <%= f.label :user_id %><br>
   <%= f.select :user_id, User.all.map {|p| [p.user_name, p.id]} %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
   <%= f.select :position, 1..@project_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
