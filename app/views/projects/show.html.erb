<p class="notice"><%= notice %></p>
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <p class="navbar-text">Currently logged in as: <b><%= session[:user_name].upcase %></b></p>
        </div>
                <div id="navbar" class="navbar-collapse collapse">
                  <form class="navbar-form navbar-right">
                    <div class="form-group">
            <%= form_tag(:controller => 'access', :action => 'attempt_login') do %>
            
              <%= text_field_tag 'user_name', nil, class: "form-control", placeholder: "Username" %>
            </div>
            <div class="form-group">
              <%= password_field_tag 'password', nil, class: "form-control", placeholder: "Password" %>
            </div>
            <%= submit_tag "Log In", class: "btn btn-success" %>
              <% end %>
          </form>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>
<div class="jumbotron">
  <div class="container">
    <h1>Project Details</h1>
  </div>
</div>
<div class="container">
<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>Notes:</strong>
  <%= @project.notes %>
</p>

<%= link_to 'Edit', edit_project_path(@project), class: "btn btn-md btn-success" %> 
<%= link_to "Back to Projects", {:action => 'index', :user_id => @project.user_id}, class: "btn btn-md btn-default" %>
</div>